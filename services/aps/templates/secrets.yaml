{{- include "factory.headers" . }}
---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "factory.name" . }}-credentials
  namespace: {{ .Release.Namespace }}
stringData:
  APS_SERVICE_USERNAME: {{ .Values.secret.serviceUsername }}
  APS_SERVICE_PASSWORD: {{ .Values.secret.servicePassword }}
