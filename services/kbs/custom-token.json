{
  "subject": "{{ .Values.customToken.subject }}",
  "validity_seconds": {{ .Values.customToken.validitySeconds }},
  "claims": {
    "permissions": [
      {
        "service": "APS",
        "rules": [
          "attestation_token:create"
        ]
      }
    ]
  }
}